import React from 'react';
import ReactDOM from 'react-dom';

const App = () => (
  <div>
    <h1>Prompetize Popup</h1>
    <p>Your prompt templates are just a click away.</p>
  </div>
);

ReactDOM.render(<App />, document.getElementById('root'));
